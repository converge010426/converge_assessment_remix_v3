export function installHeroFinalFix(): void {
  // Legacy imperative hero DOM manipulation has been permanently retired.
  // The hero is React-owned and must never be moved or wrapped outside React.
  return;
}
