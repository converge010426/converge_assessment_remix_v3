// Retained as a compatibility entry point for main.tsx.
// The hero must remain React-owned; no DOM re-parenting or overlay mutation is permitted here.
export function installHeroFinalFix(): void {
  // Intentionally no-op.
}
